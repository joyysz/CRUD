<Window x:Class="CRUD.Cadastro"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Background="#1E1E1E">
        <StackPanel Margin="20">
            <TextBlock Foreground="AliceBlue" Text="Digite seu nome:" />
            <TextBox x:Name="txtNome" Height="35" Margin="0,20,0,0" />
            <TextBlock Foreground="AliceBlue" Text="Digite seu username:" />
            <TextBox x:Name="txtUsername" Height="35" Margin="0,20,0,0" />
            <TextBlock Foreground="AliceBlue" Text="Digite seu email:" />
            <TextBox x:Name="txtEmail" Height="35" Margin="0,20,0,0" />
            <TextBlock Foreground="AliceBlue" Text="Digite sua senha:" />
            <PasswordBox x:Name="txtSenha" Height="35" Margin="0,20,0,0" />

            <Button Content="Cadastrar" Background="#7C3AED" Foreground="AliceBlue" Height="35"
                    Click="BtnCadastrar_OnClick" Margin="0, 20, 0, 0" />
        </StackPanel>
    </Grid>
</Window>